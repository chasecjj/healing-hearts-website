# Healing Hearts Website — environment variable reference
# Copy to .env.local for local development. Never commit .env.local.

# Supabase (existing)
VITE_SUPABASE_URL=https://qleojrlqnbiutyhfnqgb.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key from Supabase dashboard>
SUPABASE_URL=https://qleojrlqnbiutyhfnqgb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service role key from Supabase dashboard>

# Phedris proxy (NEW — portal-assistant-tab-v1, locked-api-contract §2.5)
# Both vars must be set in Vercel project settings AND in .env.local for local dev.
PHEDRIS_API_URL=https://api.healingheartscourse.com
PHEDRIS_SERVICE_KEY=<shared secret matching phedris_service_key in Phedris server/config.py>
